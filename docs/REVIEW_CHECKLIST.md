Review checklist — 2026-09-16

This is the current checklist for the isolated review branch `codex/mysti-hosted-ci-review-2026-09-12`, starting from `b15eefbb967db60aeec6092a9ed6f13d23f9e08a`. The September 8 review remains a historical snapshot. This round reviews coordinator dispatch/delegation, Canvas and chat lifecycle integration, editor acceptance enforcement, package/runtime evidence and outstanding external gates.

The baseline has 15 successful required hosted checks, five reviewed archives and zero full/production dependency audit findings. Those results belong to the baseline commit; implementation below requires new validation before its status can be carried forward.

| ID | Item | Current status and evidence |
| --- | --- | --- |
| R1 | Review coordinator dispatch and delegation | Review complete. Confirmed uncounted cross-review, false-clean diagnostics, and lost native side-effect provenance before reroute. Native approval/Stop and background ownership gaps also recorded below. Project command injection was investigated but not reproduced: the scanner emits fixed commands. |
| R2 | Review Canvas lifecycle | Reproduced stale switch completion after outgoing save: a closed/reopened view can receive the old switch's artifact/history/MCP link. Fixed with session/switch ownership and deferred-promise regressions; focused tests pass; current candidate gates are tracked in R7. |
| R3 | Review chat rendering lifecycle | Reproduced late tool-use followed by result after Stop: intake reopens a card and applies edit/Todo effects. Fixed with explicit live intake; unit and real Chromium regressions pass; current candidate gates are tracked in R7. |
| R4 | Review installed-editor acceptance enforcement | Reproduced QUnit focus excluding a failing required case, and arbitrary skipped cases accepted alongside passes. Hardened focus/filter/required-case/skip enforcement; 28 actual child-process probes pass on development Node and exact Node 18.17.1. |
| R5 | Dependency/runtime/package evidence | Fresh full and production audits remain zero. Baseline exact 15 check contexts and strict active rule independently rechecked. Minimum runtime declaration limits remain explicit. |
| R5a | Preserve side effects before coordinator reroute | Confirmed: native approval can precede effect notification; terminal pool results omit possible effects, allowing unsafe reroute. Fixed with per-dispatch monotonic effect provenance and crash-after-approval regressions. Explicit preflight skips alone avoid budget charges; later authentication failures consume a slot. |
| R5b | Stop after local approval/checkpoint | Confirmed: local execution can proceed after Stop while an asynchronous gate/checkpoint is pending. Fixed with callback/signal checks after preparation, gate and checkpoint awaits, including partial-patch reporting. Active process cancellation is tracked in R5b-2. |
| R5b-2 | Stop during active sandbox execution | Follow-up review confirmed no active process cancellation and direct-child-only timeout. Implemented run-wide abort and owned process-tree cleanup with actual subprocess regressions. Bounded exceptional cleanup fails explicitly and preserves scratch state; stronger orphan containment remains E7. |
| R5b-3 | Stop during preflight | Fixed: controller registration now precedes model/tool preflight awaits, and identity-based cleanup covers every early return. A retry UI action cannot revive the stopped run by clearing its transient flag. |
| R5c | Background skill/publish approval ownership | Confirmed: host drops tool/owner IDs for these ports; background Stop can miss panel-owned cards. Fixed: skill/publish gates retain tool and job owner IDs, and job Stop dismisses owned cards even when no delegation is running. Sibling cards remain pending. |
| R5d | Windows editor launcher paths | Confirmed library boundary uses a shell without quoting space-containing args. Installation now uses literal argv to the downloaded editor binary. Initial hosted acceptance also exposed VS Code 1.138's versioned Windows CLI directory and a host-dependent path test. Resolve the shipped wrapper's active CLI entry and enforce explicit platform path semantics; exact-candidate editor CI remains required. |
| R6 | Next implementation: coordinator delegation and orchestration owners | Implemented and independently reviewed: `CoordinatorDelegationRunner` owns routing, safe reroute, fold/tier state, truthful verification and budgeted review. `CoordinatorRunOrchestrator` owns serial handoffs and completion/rescue. Host permissions, cancellation identities and persistence remain explicit ports. |
| R7 | Verify revised candidate | Required per candidate: focused failure/cancellation tests, integrated host tests, typecheck, lint, full browser suite, build, exact packages/editor checks and all 15 hosted checks. Latest verified `ffe7071` passed all 15 checks and five independent archive reviews; evidence in `out-test/release-evidence/CHAT_HOST_FIX_20260920/REVIEW.json`. Current R9b validation uses `CHAT_TIMELINE_FIX_20260922/`; baseline green does not transfer to later commits. |
| R8 | Canvas tool and remaining view lifecycle extraction | Artifact owner and cross-view save/delete/restore ordering implemented and independently reviewed; failed loads/saves retain the current design, and invalid save destinations reject. 253 focused cases and fc571a5 packaged editor gates pass. Tool-session implementation and independent review pass 342 focused cases: delayed-open Stop/replacement, captured run-approval limits, and exactly one terminal per job. Candidate `98fbfa0` passes local 14,508 tests/one unavailable fixture skip, all 15 hosted gates and five independent archive reviews in CANVAS_TOOL_FIX_20260920; parser/media/export ownership and failed-close recovery remain open. |
| R9 | Remaining chat timeline and interaction extraction | Ten real Chromium witnesses cover late text/thinking/tool intake, missing final-only answers and duplicate completion draining two queued sends. Four real-host witnesses show stale ordinary-provider text/done/error/EOF effects on a replacement turn. R9a implementation and independent review pass 33 new host and 29 new compaction cases. Captured ownership prevents stale output/cleanup; guarded commits preserve newer history/memory and reject incomplete summaries. Candidate `ffe7071` passes local 14,570 tests/one unavailable Claude skip, all 15 hosted gates and five independent archive reviews. R9b timeline/request identity passes independent source review and local 14,677 tests/one unchanged skip, with 107 additions and no baseline cases removed. Exact release package and private editor acceptance pass; hosted acceptance is in progress. Three deferred visual witnesses confirm stale follow-ups after Stop, conversation switch and panel replacement; R9c is next. Immediate compaction transport cancellation remains open. |
| R10 | Minimum-Node declaration project | Complete for `ffe7071`: pinned Node 18.17/TypeScript 5.6 project covers 243 files across both host entry graphs, exact bundled fetch declarations and negative controls. All three hosted declaration gates, 23 exact Node 18.17.1 runtime checks, 28 runner probes and minimum/stable packaged editor checks pass. Revalidate subsequent changes; E3 remains separate. |
| R11 | OpenRouter ownership and complete response handling | Complete for cfe0b0d: 206 focused tests, local 14,294 passed/one skip, all 15 hosted gates and five independently reviewed archives. Opaque reasoning replay, direct-chat model/cost UI and account acceptance remain separate. |
| R12 | Qwen/Gemini configuration compatibility | Implementation and independent review pass: 135 focused tests and 93 configuration/launch tests. Final environment agrees with captured settings; supported Qwen protocol aliases accepted. Combined local validation: 14,459 source tests pass/one unavailable fixture skip; private packaged editor 13 passed/one expected native absence skip. Fresh `98fbfa0` passes all 15 hosted gates and five archive reviews after fixture cleanup. Failed aa759a0 evidence preserved; version pins and native acceptance unchanged. |
| R13 | Gateway response integrity | Six loopback witnesses reached dispatch on incomplete/malformed/error responses. Strict framing, complete tool-batch validation and per-yield Stop guards implemented; 376 focused/143 independent tests pass. Fresh `98fbfa0` local/source/package/editor validation, all 15 hosted gates and five archive reviews pass. Failed aa759a0 evidence preserved. |
| R14 | Hosted worker teardown | aa759a0 Ubuntu ran all assertions successfully but failed on pending console RPC during worker shutdown. A real agent-catalogue initializer outlived its test harness; isolated catalogue and awaited cleanup fix passes 46 focused cases, including deterministic held-initializer regression. Complete on `98fbfa0`: 46 fixture cases pass on all three platforms without unhandled errors; local 14,508 tests, all 15 hosted gates and five archive reviews pass. Failed evidence preserved; no rerun counted as validation. |
| E1 | Desk private relay and two-machine acceptance | Blocked by unavailable environment, explicitly confirmed by user. Local/native evidence does not complete it; remote task execution remains unintegrated. |
| E2 | Provider native initialization and authenticated acceptance | Open: successful Codex initialization; unavailable Hermes/Kimi/Continue and pinned Claude; actual account turns. Existing native fixtures prove only their documented scope. |
| E3 | Minimum VS Code 1.86.0 macOS GUI startup | Open: pre-workbench crash persists under isolated controls; Linux minimum success is separate. |
| E4 | Restricted provider functionality | Open: writable Copilot approval, OpenCode shell and restricted Cursor/Continue remain unsupported. |
| E5 | Real-profile downgrade acceptance | Open. Actual v0.4.0 source probes show schema/field loss; matching private snapshots remain necessary. |
| E6 | Merge, tag and extension release | Outside current authorization; candidate-branch development/publication is authorized. |
| E7 | Stronger orphan-process containment | Open platform limitation: escaped POSIX descendants or a Windows parent that exits before tree kill can prevent confirmation of descendant termination. The new watchdog fails explicitly and preserves scratch state instead of hanging; it does not report cleanup success. |

Detailed baseline evidence is in `out-test/release-evidence/ALL_REMAINING_20260914/`; this round's reproductions and results go in `out-test/release-evidence/REVIEW_ROUND_20260916/`. No real provider requests, normal-profile changes, provider installations or credential-store inspection are part of this review.
